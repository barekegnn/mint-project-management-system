# ============================================
# 🗄️ DATABASE CONFIGURATION
# ============================================
# PostgreSQL connection string for Neon database
# Get this from: https://neon.tech (free tier, no credit card)
# Format: postgresql://[user]:[password]@[host]/[database]?sslmode=require
DATABASE_URL="postgresql://user:password@ep-xxx-xxx.region.aws.neon.tech/dbname?sslmode=require"

# ============================================
# 🔐 AUTHENTICATION SECRETS
# ============================================
# Generate these using: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
# IMPORTANT: Use different secrets for each environment (dev, staging, production)

# JWT secret for token signing (32+ characters recommended)
JWT_SECRET="your-jwt-secret-here-min-32-chars"

# NextAuth secret for session encryption (32+ characters recommended)
NEXTAUTH_SECRET="your-nextauth-secret-here-min-32-chars"

# Application URL (update for production deployment)
# Local: http://localhost:3000
# Production: https://your-app.vercel.app
NEXTAUTH_URL="http://localhost:3000"

# ============================================
# 📧 EMAIL/SMTP CONFIGURATION
# ============================================
# Gmail SMTP configuration (free)
# Setup instructions:
# 1. Create new Gmail account
# 2. Enable 2-Factor Authentication
# 3. Generate App Password: https://myaccount.google.com/apppasswords
# 4. Use the 16-character app password below

# Gmail SMTP host (always smtp.gmail.com for Gmail)
SMTP_HOST="smtp.gmail.com"

# Gmail SMTP port (465 for SSL, 587 for TLS)
SMTP_PORT="465"

# Use secure connection (true for port 465, false for port 587)
SMTP_SECURE="true"

# Your Gmail email address
SMTP_USER="your-email@gmail.com"

# Your Gmail app password (16 characters, no spaces)
SMTP_PASSWORD="your-16-char-app-password"

# Email "From" address and name
SMTP_FROM="Project Management System <your-email@gmail.com>"

# Legacy email variables (for compatibility)
EMAIL_USER="your-email@gmail.com"

# Your Gmail app password (same as SMTP_PASSWORD)
EMAIL_PASSWORD="your-16-char-app-password"

# ============================================
# 📦 FILE STORAGE CONFIGURATION
# ============================================
# Vercel Blob storage token (free 1GB)
# Get this from: Vercel Dashboard > Storage > Create Blob Store
# Leave empty to disable file uploads temporarily
BLOB_READ_WRITE_TOKEN=""

# Feature flag to enable/disable file uploads
ENABLE_UPLOADS="false"

# AWS S3 Configuration (OPTIONAL - only if using S3 instead of Vercel Blob)
# Region where your S3 bucket is located
AWS_REGION="us-east-1"

# AWS Access Key ID (get from AWS IAM)
AWS_ACCESS_KEY_ID="your-aws-access-key-id"

# AWS Secret Access Key (get from AWS IAM)
AWS_SECRET_ACCESS_KEY="your-aws-secret-access-key"

# S3 bucket name for file storage
AWS_S3_BUCKET="your-bucket-name"

# ============================================
# 🔧 OPTIONAL CONFIGURATION
# ============================================
# Node environment (development, production, test)
NODE_ENV="development"

# Enable email sending (set to false to disable emails in development)
ENABLE_EMAIL="true"

# Public app URL (for client-side use)
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# Vercel Analytics ID (automatically set by Vercel in production)
# VERCEL_ANALYTICS_ID=""

# ============================================
# 📝 NOTES
# ============================================
# - NEVER commit the actual .env file to git
# - Use different secrets for each environment
# - Keep production credentials secure
# - Update NEXTAUTH_URL when deploying to production
# - Test all services locally before deploying
